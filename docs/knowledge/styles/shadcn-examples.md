# shadcn-svelte examples (deprecated)
Icon: tabler:layout-grid
Tags: deprecated, ui, references
Date: 2026-01-17
Summary: Legacy reference for the old shadcn-svelte submodule. The starter now uses local Bits UI wrappers and SCSS blocks.

- The `frontend/src/references/shadcn-svelte` submodule was removed in DEV-1234.
- Use local UI wrappers in `frontend/src/lib/components/ui/` and blocks in `frontend/src/lib/components/blocks/`.
- For layout inspiration, reference the Components Godmode window (`frontend/src/components/`).
