# MM-R CLI Command Reference

Generated: `2026-02-17T23:14:20.03018Z`

## Source of Truth

- Command model: `backend/src/cli/args.rs`
- GraphQL operation catalog: runtime schema introspection
- Generated by: `mm-cli docs generate`

## Command Tree

### `mm-cli`

MM-R CLI for backend operations, settings, and observability

Usage:

```bash
Usage: mm-cli [OPTIONS] <COMMAND>
```

Flags:

- `--app-dir` <PATH> - Override the application data directory
- `--output` <OUTPUT> - Output format for command results

#### `mm-cli settings`

Usage:

```bash
Usage: settings <COMMAND>
```

##### `mm-cli settings get`

Usage:

```bash
Usage: get [OPTIONS]
```

Flags:

- `-k, --key` <KEY> - Restrict to selected keys

##### `mm-cli settings set`

Usage:

```bash
Usage: set --set <KEY=VALUE>
```

Flags:

- `-s, --set` <KEY=VALUE> (required) - Set one or more settings

#### `mm-cli avatar`

Usage:

```bash
Usage: avatar <COMMAND>
```

##### `mm-cli avatar save`

Usage:

```bash
Usage: save --source <PATH>
```

Flags:

- `-s, --source` <PATH> (required)

#### `mm-cli database`

Usage:

```bash
Usage: database <COMMAND>
```

##### `mm-cli database export`

Usage:

```bash
Usage: export --target <PATH>
```

Flags:

- `-t, --target` <PATH> (required)

#### `mm-cli graphql`

Usage:

```bash
Usage: graphql <COMMAND>
```

##### `mm-cli graphql execute`

Usage:

```bash
Usage: execute [OPTIONS]
```

Flags:

- `--query` <GRAPHQL>
- `--query-file` <FILE>
- `--variables` <JSON>
- `--operation-name` <NAME>
- `--timeout-secs` <SECONDS> - Timeout for one-shot GraphQL execution

##### `mm-cli graphql subscribe`

Usage:

```bash
Usage: subscribe [OPTIONS]
```

Flags:

- `--query` <GRAPHQL>
- `--query-file` <FILE>
- `--variables` <JSON>
- `--operation-name` <NAME>
- `--timeout-secs` <SECONDS> - Timeout for one-shot GraphQL execution
- `--max-events` <COUNT> - Stop after receiving this many payloads

##### `mm-cli graphql operations`

Usage:

```bash
Usage: operations [OPTIONS]
```

Flags:

- `--with-args`

#### `mm-cli dashboard`

Usage:

```bash
Usage: dashboard [OPTIONS]
```

Flags:

- `--refresh-ms` <MS>
- `--limit` <COUNT>

#### `mm-cli docs`

Usage:

```bash
Usage: docs <COMMAND>
```

##### `mm-cli docs generate`

Usage:

```bash
Usage: generate [OPTIONS]
```

Flags:

- `--out` <PATH>

#### `mm-cli api`

Usage:

```bash
Usage: api <COMMAND>
```

##### `mm-cli api serve`

Usage:

```bash
Usage: serve [OPTIONS]
```

Flags:

- `--timeout-secs` <SECONDS>

#### `mm-cli vfs`

Usage:

```bash
Usage: vfs <COMMAND>
```

##### `mm-cli vfs list`

Usage:

```bash
Usage: list [OPTIONS]
```

Flags:

- `--parent` <ID> - Filter by parent folder ID
- `--taxonomy` <ID> - Filter by taxonomy ID(s)

##### `mm-cli vfs ingest`

Usage:

```bash
Usage: ingest [OPTIONS] <PATH>
```

Flags:

- `path` <PATH> (required)
- `-n, --name` <NAME>
- `--managed` - Copy to managed storage

##### `mm-cli vfs ingest-folder`

Usage:

```bash
Usage: ingest-folder [OPTIONS] <PATH>
```

Flags:

- `path` <PATH> (required)
- `--managed` - Copy to managed storage
- `-d, --depth` <DEPTH>

#### `mm-cli taxonomy`

Usage:

```bash
Usage: taxonomy <COMMAND>
```

##### `mm-cli taxonomy list`

Usage:

```bash
Usage: list [OPTIONS]
```

Flags:

- `--kind` <KIND> - Filter by kind (tag|collection)

##### `mm-cli taxonomy create`

Usage:

```bash
Usage: create [OPTIONS] <KIND> <NAME>
```

Flags:

- `kind` <KIND> (required) - tag or collection
- `name` <NAME> (required)
- `--color` <HEX>

##### `mm-cli taxonomy add`

Usage:

```bash
Usage: add <NODE_ID> <TAXONOMY_ID>
```

Flags:

- `node_id` <NODE_ID> (required)
- `taxonomy_id` <TAXONOMY_ID> (required)

##### `mm-cli taxonomy remove`

Usage:

```bash
Usage: remove <NODE_ID> <TAXONOMY_ID>
```

Flags:

- `node_id` <NODE_ID> (required)
- `taxonomy_id` <TAXONOMY_ID> (required)

#### `mm-cli metadata`

Usage:

```bash
Usage: metadata <COMMAND>
```

##### `mm-cli metadata get`

Usage:

```bash
Usage: get <NODE_ID>
```

Flags:

- `node_id` <NODE_ID> (required)

##### `mm-cli metadata set-rating`

Usage:

```bash
Usage: set-rating <NODE_ID> <RATING>
```

Flags:

- `node_id` <NODE_ID> (required)
- `rating` <RATING> (required) - 0-5 integer

#### `mm-cli prompt`

Usage:

```bash
Usage: prompt <COMMAND>
```

##### `mm-cli prompt explore`

Usage:

```bash
Usage: explore [OPTIONS]
```

Flags:

- `--provider` <PROVIDER>
- `--api-key` <API_KEY> - Override API Key

##### `mm-cli prompt create`

Usage:

```bash
Usage: create [OPTIONS] --endpoint-id <ID> --model <MODEL> <TEXT>
```

Flags:

- `-c, --connection-id` <ID>
- `-p, --provider-id` <ID>
- `-e, --endpoint-id` <ID> (required)
- `-m, --model` <MODEL> (required)
- `prompt_text` <TEXT> (required)
- `--attachments` <JSON>

##### `mm-cli prompt run`

Usage:

```bash
Usage: run <PROMPT_ID>
```

Flags:

- `prompt_id` <PROMPT_ID> (required)

## GraphQL Operations

### Query

- `adminJobEvents` (tenantId: String, jobId: String, limit: Int!)
- `adminJobs` (tenantId: String, status: String, limit: Int!)
- `connection` (id: String!)
- `connectionTypes`
- `connections` (first: Int!, after: String, includeArchived: Boolean)
- `documents` (first: Int!, after: String)
- `files` (parentId: ID, taxonomyIds: [ID!])
- `health`
- `prompt` (id: String!)
- `promptConnectionDefaults` (providerId: String, first: Int)
- `promptProviderTypes`
- `promptResponse` (id: String!)
- `promptResponses` (first: Int!, after: String, promptId: String, runId: String)
- `promptRun` (id: String!)
- `promptRuns` (first: Int!, after: String, promptId: String)
- `prompts` (first: Int!, after: String, includeArchived: Boolean)
- `suggestTaxonomies` (prefix: String!, kind: String)
- `taxonomies` (kind: String)
- `tenant` (id: String!)
- `viewer`

### Mutation

- `addTaxonomyToNode` (nodeId: ID!, taxonomyId: ID!)
- `archiveConnection` (id: String!, expectedVersion: Int!, clientMutationId: String)
- `cancelJob` (jobId: String!)
- `clearPromptConnectionDefault` (input: ClearPromptConnectionDefaultInput!)
- `createConnection` (input: CreateConnectionInput!)
- `createDocument` (input: CreateDocumentInput!)
- `createPrompt` (input: CreatePromptInput!)
- `createPromptResponse` (input: CreatePromptResponseInput!)
- `createPromptRun` (input: CreatePromptRunInput!)
- `createPromptRuns` (input: CreatePromptRunsInput!)
- `createTaxonomy` (kind: String!, name: String!, color: String)
- `ingestFolder` (path: String!, isManaged: Boolean!, maxDepth: Int)
- `ingestLocalFile` (path: String!, name: String!, isManaged: Boolean!)
- `removeTaxonomyFromNode` (nodeId: ID!, taxonomyId: ID!)
- `retryJob` (jobId: String!)
- `setNodeRating` (nodeId: ID!, rating: Int!)
- `setPromptConnectionDefault` (input: SetPromptConnectionDefaultInput!)
- `updateConnection` (id: String!, expectedVersion: Int!, input: UpdateConnectionInput!)
- `updateDocument` (id: String!, expectedVersion: Int!, input: UpdateDocumentInput!)
- `validateConnection` (id: String!, expectedVersion: Int!, clientMutationId: String)

### Subscription

- `connectionChanged` (tenantId: String!, sinceChangeId: Int)
- `documentChanged` (tenantId: String!, sinceChangeId: Int)
- `promptChanged` (tenantId: String!, sinceChangeId: Int)
- `promptResponseChanged` (tenantId: String!, sinceChangeId: Int)
- `promptRunChanged` (tenantId: String!, sinceChangeId: Int)

## Examples

- **Execute query**
  ```bash
cargo run --manifest-path backend/Cargo.toml --bin mm-cli -- graphql execute --query "query { health { queueDepth at } }"
  ```
- **Execute mutation from file**
  ```bash
cargo run --manifest-path backend/Cargo.toml --bin mm-cli -- graphql execute --query-file queries/create-document.graphql --variables '{"input":{"title":"Doc","content":{"schemaVersion":1,"data":{"ops":[]}}}}'
  ```
- **Stream subscription**
  ```bash
cargo run --manifest-path backend/Cargo.toml --bin mm-cli -- graphql subscribe --query-file queries/changes.graphql --max-events 5
  ```

